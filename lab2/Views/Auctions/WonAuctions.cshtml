@model IEnumerable<lab2.ViewModels.AuctionVM>

@{
    ViewData["Title"] = "Won auctions";
}

<h1>Won auctions</h1>

<style>
    .table td {
        vertical-align: middle;
    }
</style>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Auctioneer)
            </th>
            <th>
                Starting Price
            </th>
            <th>
                Winning Bid
            </th>
            <th>
                End Date
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Auctioneer)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.StartingPrice) kr
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.HighestBid) kr
                </td>
                <td>
                    @Convert.ToDateTime(item.EndDate).ToString("yyyy-MM-dd HH:mm")
                </td>
                <td>
                    <form asp-action="Details" asp-route-id="@item.Id" method="get" style="display: inline;">
                        <button type="submit" class="btn btn-primary">View Details</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>